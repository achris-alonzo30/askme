import { cn } from "@/lib/utils";

interface MessageProps {
    message: string;
    isUser: boolean;
}

export const Message = ({
    message,
    isUser
}: MessageProps) => {
    return (
        <div className={cn("")}>

        </div>
    )
}